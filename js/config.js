/* jsonbin.io cloud sync config — committed so the deployed site can sync.
 * IMPORTANT: only a SCOPED Access Key is here (read+write to this one bin).
 * The full-account Master Key is intentionally NOT included — never publish it. */
window.IPT_CONFIG = {
  JSONBIN_ACCESS_KEY: '$2a$10$5D710/L/iBl.cYadEqSsOucIUo8uAg/ZmPbvLaQB3HbN5PwgjXjBe',
  JSONBIN_MASTER_KEY: '',
  JSONBIN_BIN_ID: '6a42892af5f4af5e29423a1c',
  JSONBIN_BASE: 'https://api.jsonbin.io/v3/b'
};
