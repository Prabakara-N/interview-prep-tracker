# NOTE: js/config.js is intentionally committed so the deployed site can sync.
# It contains ONLY a scoped Access Key (this bin, read+write) — never the Master Key.

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
